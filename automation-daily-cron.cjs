const base=process.env.DAILY_WEB_URL||process.env.RENDER_EXTERNAL_URL;const target=process.env.AUTOMATION_DAILY_TIME||"19:00";const now=new Intl.DateTimeFormat("es-ES",{timeZone:"Europe/Madrid",hour:"2-digit",minute:"2-digit",hour12:false}).format(new Date());if(now!==target){console.log("No toca publicar:",now,"objetivo:",target);process.exit(0)}if(!base)throw Error("Falta DAILY_WEB_URL");const r=await fetch(base.replace(/\/$/,"")+"/api/automation/daily",{method:"POST",headers:{"x-automation-secret":String(process.env.AUTOMATION_SECRET||"")}});const t=await r.text();console.log(t);if(!r.ok)process.exit(1);