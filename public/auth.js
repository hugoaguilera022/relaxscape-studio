(()=>{
 const gate=document.querySelector("#authGate"),login=document.querySelector("#loginGoogle"),err=document.querySelector("#authError");
 const show=()=>{if(gate)gate.style.display="flex"};
 const hide=()=>{if(gate)gate.style.display="none"};
 async function status(){try{const r=await fetch("/api/auth/status",{cache:"no-store",credentials:"same-origin"});const x=await r.json();if(x.authenticated){hide();window.RelaxScapeUser=x.user;setAccount(x.user);await loadPrefs()}else show()}catch(e){show();if(err)err.textContent="No se pudo comprobar la sesión."}}
 function setAccount(u){const name=u?.name||"Cuenta Google",email=u?.email||"Cuenta";const initial=(name.trim()[0]||"G").toUpperCase();["accountAvatar","accountAvatarLarge","settingsAvatar"].forEach(id=>{const el=document.getElementById(id);if(el)el.textContent=initial});["accountEmail","accountEmailLarge","settingsEmail"].forEach(id=>{const el=document.getElementById(id);if(el)el.textContent=email});["accountName","settingsName"].forEach(id=>{const el=document.getElementById(id);if(el)el.textContent=name})}
 async function logout(){try{await fetch("/api/auth/logout",{method:"POST",credentials:"same-origin"})}finally{location.reload()}}
 async function loadPrefs(){try{const r=await fetch("/api/user/preferences",{cache:"no-store"});if(!r.ok)return;const x=await r.json();const p=x.preferences||{};for(const [id,v] of Object.entries(p)){const el=document.getElementById(id);if(!el)continue;if(el.type==="checkbox")el.checked=!!v;else if(el.type!=="file")el.value=String(v)};localStorage.setItem("relaxscape.preferences",JSON.stringify(p))}catch{}}
 let timer;
 async function savePrefs(){if(gate?.style.display!=="none")return;const p={};document.querySelectorAll("input[id],textarea[id],select[id]").forEach(el=>{if(el.type==="file")return;if(el.type==="checkbox")p[el.id]=el.checked;else if(el.type!=="password")p[el.id]=el.value});try{await fetch("/api/user/preferences",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify({preferences:p})})}catch{}}
 document.addEventListener("DOMContentLoaded",()=>{if(login)login.onclick=()=>location.href="/api/auth/login";const menu=document.getElementById("accountMenu"),btn=document.getElementById("accountButton");if(btn)btn.onclick=()=>menu?.classList.toggle("open");document.addEventListener("click",e=>{if(menu&&!menu.contains(e.target))menu.classList.remove("open")});const openSettings=()=>{document.getElementById("settingsModal")?.classList.remove("hidden");menu?.classList.remove("open")};document.getElementById("accountClose")?.addEventListener("click",()=>menu?.classList.remove("open"));document.getElementById("accountSettings")?.addEventListener("click",openSettings);document.getElementById("closeSettings")?.addEventListener("click",()=>document.getElementById("settingsModal")?.classList.add("hidden"));document.getElementById("settingsYoutube")?.addEventListener("click",()=>{document.getElementById("settingsModal")?.classList.add("hidden");document.querySelector('[data-tab="youtubePublish"]')?.click()});document.getElementById("accountLogout")?.addEventListener("click",logout);document.getElementById("settingsLogout")?.addEventListener("click",logout);status();timer=setInterval(savePrefs,5000);document.addEventListener("change",savePrefs);document.addEventListener("input",()=>{clearTimeout(window.__rsSave);window.__rsSave=setTimeout(savePrefs,1200)})});
})();